# Copy to .env.github and fill in (never commit .env.github, never paste token in chat)
# Create token: GitHub → Settings → Developer settings → Personal access tokens → classic → repo scope

GITHUB_TOKEN=ghp_your_token_here
GITHUB_USER=aicastingcom
GITHUB_REPO=cinematicviewing
